r"""
This package brings functions for image loading and manipulation.

content (files):
    - __init__.py: Initialize the package IO. 

    - distance.py: functions that compute distances 
        dist_manhattan(), dist_euclidean(), dist_colorweight()
    - load_image.py: functions that load an image 
        check_filepath(filename): checks if filename is a valid file path 
        

import commands:
    - import IO.load_image as loader
"""
